# Dépendances pour l'extraction intelligente de texte
# Installation : pip install -r requirements_extraction.txt

# Extraction PDF avancée
pdfplumber>=0.10.0

# OCR (Optical Character Recognition)
pytesseract>=0.3.10
pdf2image>=1.16.3
Pillow>=10.0.0

# Vision API (optionnel)
openai>=1.0.0

# Note: Tesseract OCR doit être installé séparément
# macOS: brew install tesseract tesseract-lang
# Ubuntu: sudo apt-get install tesseract-ocr tesseract-ocr-ara tesseract-ocr-fra
# Windows: Télécharger depuis https://github.com/UB-Mannheim/tesseract/wiki

# Note: pdf2image nécessite poppler-utils
# macOS: brew install poppler
# Ubuntu: sudo apt-get install poppler-utils
# Windows: Télécharger depuis http://blog.alivate.com.au/poppler-windows/
